import { PendingScreen } from '@/components/PendingScreen';

// Modal target for dashboard taps whose real surfaces ship after approval.
export default function PendingRoute() {
  return <PendingScreen />;
}

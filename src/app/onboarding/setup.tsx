import { SetupWizard } from '@/features/onboarding/ui/SetupWizard';

/** S-00b Optional setup wizard — every step skippable, exit anytime. */
export default function SetupRoute() {
  return <SetupWizard />;
}

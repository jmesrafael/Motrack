import { WelcomeScreen } from '@/features/onboarding/ui/WelcomeScreen';

/** S-00a First-launch welcome — shown once; both paths mark it completed. */
export default function WelcomeRoute() {
  return <WelcomeScreen />;
}

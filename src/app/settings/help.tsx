import { HelpScreen } from '@/features/onboarding/ui/HelpScreen';

/** S-32 Help & Tutorials — replay, Tutorial Mode, reset tutorial progress. */
export default function HelpRoute() {
  return <HelpScreen />;
}

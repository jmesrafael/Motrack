import { PendingScreen } from '@/components/PendingScreen';

// S-10 ships after dashboard approval; route exists so the tab shell is real.
export default function MaintenanceRoute() {
  return <PendingScreen />;
}

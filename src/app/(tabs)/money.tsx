import { PendingScreen } from '@/components/PendingScreen';

// S-22 ships after dashboard approval; route exists so the tab shell is real.
export default function MoneyRoute() {
  return <PendingScreen />;
}

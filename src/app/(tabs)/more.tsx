import { PendingScreen } from '@/components/PendingScreen';

// S-30 ships after dashboard approval; route exists so the tab shell is real.
export default function MoreRoute() {
  return <PendingScreen />;
}
